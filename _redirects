# Cloudflare Pages already serves page.html at /page (pretty URLs).
# Do NOT rewrite /page → /page.html — that causes infinite 308 loops
# when combined with CF's ".html extension" redirects.
#
# Optional short aliases only:
/numbers /house-numbers 301
